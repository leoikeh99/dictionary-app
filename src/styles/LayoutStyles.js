import { styled } from "styled-components";

export const Container = styled.div`
  max-width: min(46rem, 97%);
  margin-inline: auto;
`;

export const SpaceOut = styled.div`
  display: flex;
  align-items: center;
  justify-content: space-between;
`;
